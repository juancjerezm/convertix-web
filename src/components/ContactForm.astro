<section class="contact reveal" id="contactame">
  <div class="container">
    <p class="section-label">Contacto</p>
    <h2 class="section-title">Hablemos de tu proyecto</h2>
    <p class="section-subtitle">
      Escribime por WhatsApp y te respondo en menos de 2 horas.
    </p>

    <div class="whatsapp-only">
      <div class="whatsapp-card">
        <div class="whatsapp-icon">💬</div>
        <h3>Escribime ahora</h3>
        <p>
          Sin formularios, sin vueltas. Hablás directo conmigo.
        </p>
        <a
          href="https://wa.me/57316300020"
          class="btn btn-primary"
          target="_blank"
          rel="noopener"
        >
          💬 Abrir WhatsApp
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    text-align: center;
  }

  .contact .section-subtitle {
    margin: 0 auto 64px;
  }

  .whatsapp-only {
    max-width: 480px;
    margin: 0 auto;
  }

  .whatsapp-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) + 8px);
    padding: 56px 48px;
    text-align: center;
  }

  .whatsapp-icon {
    font-size: 3.5rem;
    margin-bottom: 24px;
  }

  .whatsapp-card h3 {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 12px;
  }

  .whatsapp-card p {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
    margin-bottom: 32px;
  }

  .whatsapp-card .btn {
    font-size: 1rem;
    padding: 16px 40px;
  }

  @media (max-width: 768px) {
    .whatsapp-card {
      padding: 40px 24px;
    }
  }
</style>
