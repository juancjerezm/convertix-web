<section class="pricing-banner reveal" id="precios">
  <div class="container">
    <div class="pricing-card">
      <p class="pricing-label">Precios</p>
      <p class="pricing-headline">
        Desde <span class="pricing-number">$550.000</span>
      </p>
      <p class="pricing-sub">
        Cada proyecto es distinto. Contame el tuyo y te doy un precio exacto, sin compromiso.
      </p>
      <a
        href="https://wa.me/573163000208?text=¡Hola!%20Vi%20tu%20página%20y%20quiero%20saber%20el%20precio%20de%20un%20proyecto."
        class="btn btn-primary"
        target="_blank"
        rel="noopener"
      >
        Cuéntame tu proyecto 📲
      </a>
    </div>
  </div>
</section>

<style>
  .pricing-banner {
    text-align: center;
  }

  .pricing-card {
    max-width: 520px;
    margin: 0 auto;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: calc(var(--radius) + 8px);
    padding: 48px 40px;
  }

  .pricing-label {
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--accent-orange);
    margin-bottom: 12px;
  }

  .pricing-headline {
    font-size: 1.3rem;
    font-weight: 700;
    margin-bottom: 12px;
  }

  .pricing-number {
    font-size: 2rem;
    font-weight: 800;
    color: var(--accent-orange);
    font-variant-numeric: tabular-nums;
  }

  .pricing-sub {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
    margin-bottom: 28px;
  }

  .pricing-card .btn {
    font-size: 1rem;
    padding: 14px 36px;
  }

  @media (max-width: 480px) {
    .pricing-card {
      padding: 36px 24px;
    }

    .pricing-number {
      font-size: 1.6rem;
    }
  }
</style>
