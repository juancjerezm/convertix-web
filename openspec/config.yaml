# OpenSpec Project Configuration — convertix-web
# Generated by sdd-init on 2026-06-09

context: >
  Astro 6 static site (landing page broker), pnpm, Node ≥22.12.
  Deployed on Netlify with security headers. animejs 3.2.2 for
  animations. 14 landing-section specs in openspec/specs/.
  E2E tests via Playwright with axe-core accessibility audits.
  No unit/integration test layer. No linter or formatter configured.

strict_tdd: true

testing:
  runner:
    command: "pnpm test"
    framework: "Playwright 1.60.0"
  layers:
    unit: false
    integration: false
    e2e: true
    e2e_tool: "Playwright"
  coverage:
    available: false
  quality:
    linter: false
    type_checker: true
    type_checker_tool: "astro/tsconfigs/strict"
    formatter: false
